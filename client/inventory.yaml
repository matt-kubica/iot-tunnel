all:
  hosts:
  children:
    clients:
      hosts:
        192.168.88.249:
      vars:
        ansible_connection: ssh
        ansible_ssh_user: pi
        ansible_ssh_pass: raspberry
    servers:
      hosts:
        ...:
      vars:
        ansible_connection: ssh
        ansible_ssh_user: ubuntu
