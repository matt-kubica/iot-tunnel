defaults:
  open-vpn:
    internal-network-address: 10.8.0.0
    internal-network-mask: 255.255.255.0
    external-address: 1.1.1.1
    external-port: 443

spring:
  datasource:
    url: jdbc:h2:mem:default
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

server:
  port: 5001
